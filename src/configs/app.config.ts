import { Theme } from "@/types/core.type"

export const VERSION = "0.0" as const

export const DEFAULT_THEME: Theme = "dark"

export const WALLET_CONNECT_PROJECT_ID = "" as const

export const SIGN_MESSAGE = "Address Sign" as const
